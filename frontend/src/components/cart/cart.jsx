import React from 'react';
import { Steps } from 'antd';

const { Step } = Steps;

const Cart = () => {
  onChange = current => {
    console.log('onChange:', current);
    // this.setState({ current });
  }; 
  return (
    <>
    
    </>
  )
}

export default Cart ;

